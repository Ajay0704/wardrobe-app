[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js on Netlify uses the @netlify/next runtime automatically.
# No extra redirects needed for App Router.
